import 'package:flutter/material.dart';

const double commonSize = 18;
const bgColor = Color.fromARGB(255, 100, 165, 161);
const normalStyle = TextStyle(fontSize: commonSize, color: Colors.white);
const boldStyle = TextStyle(fontSize: commonSize, color: Colors.white, fontWeight: FontWeight.bold);
